<Window x:Class="FlopManager.Services.CustomPopupActions.MyNotificationWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            MinWidth="300" MinHeight="150" 
            Title="{Binding Title}" FlowDirection="LeftToRight" WindowStyle="ToolWindow">

    <Grid x:Name="LayoutRoot" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ContentControl HorizontalAlignment="Right" VerticalAlignment="Stretch" Grid.Row="0" Content="{Binding Content}"/>

        <Button x:Name="OKButton" Content="موافق" Width="75" Height="25" HorizontalAlignment="Center" Margin="10" Grid.Row="1" Click="OKButton_Click" />

    </Grid>
</Window>
